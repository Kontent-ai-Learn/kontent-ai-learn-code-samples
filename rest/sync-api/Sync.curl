# DocSection: sync_api_synchronize_changes
curl --request GET \
  --url https://deliver.kontent.ai/<YOUR_PROJECT_ID>/early-access/sync \
  --header 'X-Continuation: <YOUR_CONTINUATION_TOKEN>'
# EndDocSection